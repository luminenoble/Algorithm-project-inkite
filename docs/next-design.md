# 下阶段的实现要求

## 故事书&

### 描述

我的故事界面里,扩展故事书功能(文件夹名),打开后显示章节(子文件夹名),
需要给所有的文章新增两个数据库字段,代表所属故事书和所属章节
如果创建时没有给出加入默认故事书的默认章节
故事书使用一个默认封面需要支持更换封面

### 前端效果:

1. 我的故事点击后进入故事书的总览,按照流式布局显示封面+书名+创建时间
支持创建时间排序 / 修改时间排序, 支持pin功能

2. 进入一本故事书后,类似文件树的布局,按行聚合不同章节名的故事,一个章节可以有多个故事